name: irontrail
metadata:
  template: irontrail-private-beta@0.1.0

infra:
  provider: bicep
  path: infra

services:
  web:
    project: .
    language: python
    host: containerapp
    docker:
      path: Dockerfile
      context: .
      platform: amd64
      remoteBuild: true
